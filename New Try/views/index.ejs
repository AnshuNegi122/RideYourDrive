<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vehicle Rental - Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <%- include('partials/header') %>
  
  <main>
    <!-- Hero Section with Booking Widget -->
    <section class="relative">
      <div class="bg-gradient-to-r from-blue-500 to-indigo-600 text-white py-20">
        <div class="container mx-auto px-4">
          <div class="flex flex-col md:flex-row items-center">
            <div class="md:w-1/2 mb-10 md:mb-0">
              <h1 class="text-4xl md:text-5xl font-bold mb-4">Rent or List Your Vehicle</h1>
              <p class="text-xl mb-8">The easiest way to rent a car or bike, or make money by listing your vehicle.</p>
              <div class="flex flex-col sm:flex-row gap-4">
                <a href="/register" class="bg-white text-blue-600 hover:bg-gray-100 font-semibold py-3 px-6 rounded-lg text-center">Get Started</a>
                <a href="#how-it-works" class="bg-transparent border-2 border-white hover:bg-white hover:text-blue-600 font-semibold py-3 px-6 rounded-lg text-center">Learn More</a>
              </div>
            </div>
            <div class="md:w-1/2">
              <img src="/images/hero-car.png" alt="Car rental" class="rounded-lg shadow-xl">
            </div>
          </div>
        </div>
      </div>
      
      <!-- Booking Widget -->
      <div class="container mx-auto px-4 relative -mt-16 z-10">
        <div class="bg-gray-800 rounded-lg shadow-xl overflow-hidden">
          <div class="p-6">
            <div class="flex justify-between items-center mb-6">
              <h2 class="text-2xl font-bold text-white">Book Now</h2>
              <div class="flex items-center text-gray-300">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-8-3a1 1 0 00-.867.5 1 1 0 11-1.731-1A3 3 0 0113 8a3.001 3.001 0 01-2 2.83V11a1 1 0 11-2 0v-1a1 1 0 011-1 1 1 0 100-2zm0 8a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" />
                </svg>
                Need help?
              </div>
            </div>
            
            <form action="/search" method="GET" class="grid grid-cols-1 md:grid-cols-5 gap-4">
              <div class="md:col-span-1">
                <label for="pickupLocation" class="block text-gray-300 mb-2">Pick Up Location</label>
                <select id="pickupLocation" name="pickupLocation" class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-blue-500">
                  <% locations.forEach(location => { %>
                    <option value="<%= location._id %>"><%= location.name %></option>
                  <% }); %>
                </select>
              </div>
              
              <div class="md:col-span-1">
                <label for="dropoffLocation" class="block text-gray-300 mb-2">Drop Off Location</label>
                <select id="dropoffLocation" name="dropoffLocation" class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-blue-500">
                  <% locations.forEach(location => { %>
                    <option value="<%= location._id %>"><%= location.name %></option>
                  <% }); %>
                </select>
              </div>
              
              <div class="md:col-span-1">
                <label for="startDate" class="block text-gray-300 mb-2">Pick Up Date</label>
                <input type="date" id="startDate" name="startDate" class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-blue-500">
              </div>
              
              <div class="md:col-span-1">
                <label for="endDate" class="block text-gray-300 mb-2">Return Date</label>
                <input type="date" id="endDate" name="endDate" class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-blue-500">
              </div>
              
              <div class="md:col-span-1">
                <label class="block text-gray-300 mb-2">&nbsp;</label>
                <button type="submit" class="w-full bg-green-500 hover:bg-green-600 text-white py-2 px-4 rounded-lg flex items-center justify-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                  </svg>
                  Find a Vehicle
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </section>
    
    <% if (typeof user !== 'undefined' && user) { %>
      <!-- Featured Vehicles Section for Logged In Users -->
      <section class="py-16 bg-white">
        <div class="container mx-auto px-4">
          <div class="flex justify-between items-center mb-8">
            <h2 class="text-3xl font-bold">Featured Vehicles</h2>
            <div class="flex space-x-2">
              <button onclick="showVehicleType('all')" class="vehicle-filter-btn bg-blue-600 text-white py-2 px-4 rounded-lg">All</button>
              <button onclick="showVehicleType('car')" class="vehicle-filter-btn bg-gray-200 text-gray-800 py-2 px-4 rounded-lg">Cars</button>
              <button onclick="showVehicleType('bike')" class="vehicle-filter-btn bg-gray-200 text-gray-800 py-2 px-4 rounded-lg">Bikes</button>
            </div>
          </div>
          
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
            <% vehicles.forEach(vehicle => { %>
              <div class="vehicle-card bg-white rounded-lg shadow-md overflow-hidden <%= vehicle.type %>-type">
                <div class="h-48 bg-gray-300">
                  <% if (vehicle.images && vehicle.images.length > 0) { %>
                    <img src="/<%= vehicle.images[0] %>" alt="<%= vehicle.make %> <%= vehicle.model %>" class="w-full h-full object-cover">
                  <% } else { %>
                    <div class="w-full h-full flex items-center justify-center text-gray-500">No image</div>
                  <% } %>
                </div>
                <div class="p-6">
                  <div class="flex justify-between items-start mb-2">
                    <h3 class="text-lg font-bold"><%= vehicle.make %> <%= vehicle.model %></h3>
                    <span class="bg-blue-100 text-blue-800 text-xs font-semibold px-2.5 py-0.5 rounded"><%= vehicle.type.charAt(0).toUpperCase() + vehicle.type.slice(1) %></span>
                  </div>
                  <p class="text-gray-600 mb-2"><%= vehicle.year %> • <%= vehicle.companyId.name %></p>
                  <p class="text-gray-700 mb-4 line-clamp-2"><%= vehicle.description %></p>
                  <div class="flex justify-between items-center">
                    <span class="text-lg font-bold text-blue-600">$<%= vehicle.price %>/day</span>
                    <a href="/vehicles/<%= vehicle._id %>" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded">View Details</a>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
          
          <div class="text-center mt-8">
            <a href="/search" class="inline-block bg-blue-600 hover:bg-blue-700 text-white py-2 px-6 rounded-lg font-semibold">View All Vehicles</a>
          </div>
        </div>
      </section>
    <% } %>
    
    <!-- How It Works Section -->
    <section id="how-it-works" class="py-16 bg-gray-100">
      <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center mb-12">How It Works</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <!-- For Renters -->
          <div class="bg-white p-8 rounded-lg shadow-md">
            <h3 class="text-2xl font-bold mb-4 text-blue-600">For Renters</h3>
            <ul class="space-y-4">
              <li class="flex items-start">
                <div class="bg-blue-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">1</div>
                <div>
                  <h4 class="font-semibold">Sign up as a Renter</h4>
                  <p class="text-gray-600">Create your account and select "Renter" as your account type.</p>
                </div>
              </li>
              <li class="flex items-start">
                <div class="bg-blue-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">2</div>
                <div>
                  <h4 class="font-semibold">Verify Your Documents</h4>
                  <p class="text-gray-600">Upload your driving license and PAN card for verification.</p>
                </div>
              </li>
              <li class="flex items-start">
                <div class="bg-blue-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">3</div>
                <div>
                  <h4 class="font-semibold">Browse and Book</h4>
                  <p class="text-gray-600">Browse available vehicles and book the one that suits your needs.</p>
                </div>
              </li>
              <li class="flex items-start">
                <div class="bg-blue-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">4</div>
                <div>
                  <h4 class="font-semibold">Enjoy Your Ride</h4>
                  <p class="text-gray-600">Once approved, pick up the vehicle and enjoy your journey.</p>
                </div>
              </li>
            </ul>
          </div>
          
          <!-- For Owners -->
          <div class="bg-white p-8 rounded-lg shadow-md">
            <h3 class="text-2xl font-bold mb-4 text-green-600">For Vehicle Owners</h3>
            <ul class="space-y-4">
              <li class="flex items-start">
                <div class="bg-green-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">1</div>
                <div>
                  <h4 class="font-semibold">Sign up as an Owner</h4>
                  <p class="text-gray-600">Create your account and select "Owner" as your account type.</p>
                </div>
              </li>
              <li class="flex items-start">
                <div class="bg-green-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">2</div>
                <div>
                  <h4 class="font-semibold">Add Your Company</h4>
                  <p class="text-gray-600">Add your company details to establish your brand.</p>
                </div>
              </li>
              <li class="flex items-start">
                <div class="bg-green-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">3</div>
                <div>
                  <h4 class="font-semibold">List Your Vehicles</h4>
                  <p class="text-gray-600">Add details and photos of the vehicles you want to rent out.</p>
                </div>
              </li>
              <li class="flex items-start">
                <div class="bg-green-600 text-white rounded-full w-8 h-8 flex items-center justify-center mr-4 flex-shrink-0">4</div>
                <div>
                  <h4 class="font-semibold">Manage Bookings</h4>
                  <p class="text-gray-600">Approve booking requests and manage your rentals.</p>
                </div>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Featured Vehicles Section for Non-Logged In Users -->
    <% if (typeof user === 'undefined' || !user) { %>
      <section class="py-16 bg-white">
        <div class="container mx-auto px-4">
          <h2 class="text-3xl font-bold text-center mb-12">Featured Vehicles</h2>
          
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
            <!-- Vehicle Card 1 -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
              <div class="h-48 bg-gray-300">
                <img src="/images/car1.jpg" alt="Car" class="w-full h-full object-cover">
              </div>
              <div class="p-6">
                <h3 class="text-xl font-bold mb-2">Toyota Camry</h3>
                <p class="text-gray-600 mb-4">Comfortable sedan for family trips or business travel.</p>
                <div class="flex justify-between items-center">
                  <span class="text-blue-600 font-bold">$50/day</span>
                  <a href="/register" class="bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">Rent Now</a>
                </div>
              </div>
            </div>
            
            <!-- Vehicle Card 2 -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
              <div class="h-48 bg-gray-300">
                <img src="/images/bike1.jpg" alt="Bike" class="w-full h-full object-cover">
              </div>
              <div class="p-6">
                <h3 class="text-xl font-bold mb-2">Honda CBR</h3>
                <p class="text-gray-600 mb-4">Sporty bike for adventure and city commuting.</p>
                <div class="flex justify-between items-center">
                  <span class="text-blue-600 font-bold">$30/day</span>
                  <a href="/register" class="bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">Rent Now</a>
                </div>
              </div>
            </div>
            
            <!-- Vehicle Card 3 -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
              <div class="h-48 bg-gray-300">
                <img src="/images/car2.jpg" alt="SUV" class="w-full h-full object-cover">
              </div>
              <div class="p-6">
                <h3 class="text-xl font-bold mb-2">Jeep Wrangler</h3>
                <p class="text-gray-600 mb-4">Rugged SUV for off-road adventures and exploration.</p>
                <div class="flex justify-between items-center">
                  <span class="text-blue-600 font-bold">$75/day</span>
                  <a href="/register" class="bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">Rent Now</a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    <% } %>
    
    <!-- Testimonials Section -->
    <section class="py-16 bg-gray-100">
      <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center mb-12">What Our Users Say</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          <!-- Testimonial 1 -->
          <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="flex items-center mb-4">
              <div class="w-12 h-12 bg-gray-300 rounded-full mr-4"></div>
              <div>
                <h4 class="font-bold">John Doe</h4>
                <p class="text-gray-600">Renter</p>
              </div>
            </div>
            <p class="text-gray-700">"The verification process was quick and the car I rented was in perfect condition. Will definitely use this service again!"</p>
          </div>
          
          <!-- Testimonial 2 -->
          <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="flex items-center mb-4">
              <div class="w-12 h-12 bg-gray-300 rounded-full mr-4"></div>
              <div>
                <h4 class="font-bold">Jane Smith</h4>
                <p class="text-gray-600">Vehicle Owner</p>
              </div>
            </div>
            <p class="text-gray-700">"I've been able to make extra income by listing my car when I'm not using it. The platform makes it easy to manage bookings."</p>
          </div>
          
          <!-- Testimonial 3 -->
          <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="flex items-center mb-4">
              <div class="w-12 h-12 bg-gray-300 rounded-full mr-4"></div>
              <div>
                <h4 class="font-bold">Mike Johnson</h4>
                <p class="text-gray-600">Renter</p>
              </div>
            </div>
            <p class="text-gray-700">"Found a great bike for my weekend trip. The booking process was seamless and the owner was very helpful."</p>
          </div>
        </div>
      </div>
    </section>
  </main>
  
  <%- include('partials/footer') %>
  
  <script>
    // Set minimum dates for booking
    document.addEventListener('DOMContentLoaded', function() {
      const today = new Date();
      const tomorrow = new Date(today);
      tomorrow.setDate(tomorrow.getDate() + 1);
      
      const startDateInput = document.getElementById('startDate');
      const endDateInput = document.getElementById('endDate');
      
      if (startDateInput && endDateInput) {
        // Format date as YYYY-MM-DD
        const formatDate = (date) => {
          const year = date.getFullYear();
          const month = String(date.getMonth() + 1).padStart(2, '0');
          const day = String(date.getDate()).padStart(2, '0');
          return `${year}-${month}-${day}`;
        };
        
        startDateInput.min = formatDate(today);
        startDateInput.value = formatDate(today);
        
        endDateInput.min = formatDate(tomorrow);
        endDateInput.value = formatDate(tomorrow);
        
        startDateInput.addEventListener('change', function() {
          const selectedDate = new Date(this.value);
          const nextDay = new Date(selectedDate);
          nextDay.setDate(nextDay.getDate() + 1);
          
          endDateInput.min = formatDate(nextDay);
          
          // If end date is before start date, reset it
          if (new Date(endDateInput.value) <= selectedDate) {
            endDateInput.value = formatDate(nextDay);
          }
        });
      }
    });
    
    // Vehicle type filter
    function showVehicleType(type) {
      const buttons = document.querySelectorAll('.vehicle-filter-btn');
      buttons.forEach(btn => {
        btn.classList.remove('bg-blue-600', 'text-white');
        btn.classList.add('bg-gray-200', 'text-gray-800');
      });
      
      const clickedButton = event.currentTarget;
      clickedButton.classList.remove('bg-gray-200', 'text-gray-800');
      clickedButton.classList.add('bg-blue-600', 'text-white');
      
      const cards = document.querySelectorAll('.vehicle-card');
      
      if (type === 'all') {
        cards.forEach(card => {
          card.style.display = 'block';
        });
      } else {
        cards.forEach(card => {
          if (card.classList.contains(type + '-type')) {
            card.style.display = 'block';
          } else {
            card.style.display = 'none';
          }
        });
      }
    }
  </script>
</body>
</html>
